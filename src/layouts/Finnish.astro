---
import { MarkdownContent } from 'astro'
import Layout from './Layout.astro'
import './alku.css'

export interface Props {
	content: MarkdownContent<{ title: string }>
}

const { title } = Astro.props.content
---

<Layout lang="fi" title={title}>
	<slot />
</Layout>
